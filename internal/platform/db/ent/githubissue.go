// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/issuesight/issuesight/internal/platform/db/ent/githubissue"
	"github.com/issuesight/issuesight/internal/platform/db/ent/project"
	"github.com/issuesight/issuesight/internal/platform/db/ent/tutorialcontent"
)

// GithubIssue is the model entity for the GithubIssue schema.
type GithubIssue struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ProjectID holds the value of the "project_id" field.
	ProjectID uuid.UUID `json:"project_id,omitempty"`
	// IssueNumber holds the value of the "issue_number" field.
	IssueNumber int `json:"issue_number,omitempty"`
	// GitHub issue ID
	GhIssueID int64 `json:"gh_issue_id,omitempty"`
	// Cached GitHub JSON response
	RawData map[string]interface{} `json:"raw_data,omitempty"`
	// LastSyncedAt holds the value of the "last_synced_at" field.
	LastSyncedAt time.Time `json:"last_synced_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the GithubIssueQuery when eager-loading is set.
	Edges        GithubIssueEdges `json:"edges"`
	selectValues sql.SelectValues
}

// GithubIssueEdges holds the relations/edges for other nodes in the graph.
type GithubIssueEdges struct {
	// Project holds the value of the project edge.
	Project *Project `json:"project,omitempty"`
	// TutorialContent holds the value of the tutorial_content edge.
	TutorialContent *TutorialContent `json:"tutorial_content,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ProjectOrErr returns the Project value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e GithubIssueEdges) ProjectOrErr() (*Project, error) {
	if e.Project != nil {
		return e.Project, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: project.Label}
	}
	return nil, &NotLoadedError{edge: "project"}
}

// TutorialContentOrErr returns the TutorialContent value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e GithubIssueEdges) TutorialContentOrErr() (*TutorialContent, error) {
	if e.TutorialContent != nil {
		return e.TutorialContent, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: tutorialcontent.Label}
	}
	return nil, &NotLoadedError{edge: "tutorial_content"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*GithubIssue) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case githubissue.FieldRawData:
			values[i] = new([]byte)
		case githubissue.FieldIssueNumber, githubissue.FieldGhIssueID:
			values[i] = new(sql.NullInt64)
		case githubissue.FieldLastSyncedAt:
			values[i] = new(sql.NullTime)
		case githubissue.FieldID, githubissue.FieldProjectID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the GithubIssue fields.
func (_m *GithubIssue) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case githubissue.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case githubissue.FieldProjectID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field project_id", values[i])
			} else if value != nil {
				_m.ProjectID = *value
			}
		case githubissue.FieldIssueNumber:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field issue_number", values[i])
			} else if value.Valid {
				_m.IssueNumber = int(value.Int64)
			}
		case githubissue.FieldGhIssueID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field gh_issue_id", values[i])
			} else if value.Valid {
				_m.GhIssueID = value.Int64
			}
		case githubissue.FieldRawData:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field raw_data", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.RawData); err != nil {
					return fmt.Errorf("unmarshal field raw_data: %w", err)
				}
			}
		case githubissue.FieldLastSyncedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_synced_at", values[i])
			} else if value.Valid {
				_m.LastSyncedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the GithubIssue.
// This includes values selected through modifiers, order, etc.
func (_m *GithubIssue) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryProject queries the "project" edge of the GithubIssue entity.
func (_m *GithubIssue) QueryProject() *ProjectQuery {
	return NewGithubIssueClient(_m.config).QueryProject(_m)
}

// QueryTutorialContent queries the "tutorial_content" edge of the GithubIssue entity.
func (_m *GithubIssue) QueryTutorialContent() *TutorialContentQuery {
	return NewGithubIssueClient(_m.config).QueryTutorialContent(_m)
}

// Update returns a builder for updating this GithubIssue.
// Note that you need to call GithubIssue.Unwrap() before calling this method if this GithubIssue
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *GithubIssue) Update() *GithubIssueUpdateOne {
	return NewGithubIssueClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the GithubIssue entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *GithubIssue) Unwrap() *GithubIssue {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: GithubIssue is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *GithubIssue) String() string {
	var builder strings.Builder
	builder.WriteString("GithubIssue(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("project_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ProjectID))
	builder.WriteString(", ")
	builder.WriteString("issue_number=")
	builder.WriteString(fmt.Sprintf("%v", _m.IssueNumber))
	builder.WriteString(", ")
	builder.WriteString("gh_issue_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.GhIssueID))
	builder.WriteString(", ")
	builder.WriteString("raw_data=")
	builder.WriteString(fmt.Sprintf("%v", _m.RawData))
	builder.WriteString(", ")
	builder.WriteString("last_synced_at=")
	builder.WriteString(_m.LastSyncedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// GithubIssues is a parsable slice of GithubIssue.
type GithubIssues []*GithubIssue
